{% extends 'base.html.twig' %}
{% block title 'Creation d\'une reservation' %}
{% block body %}

    <div class="container mt-4">
        <h1>Creation d'un utilisateur</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Créer</button>
        {{ form_end(form) }}


    </div>
{% endblock %}
